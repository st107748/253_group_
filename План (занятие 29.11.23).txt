Задачи на библиотечный класс множество


1(Мария). Написать функцию mysort, вводящую с клавиатуры набор целых чисел
 (в любом порядке), и выводящую числа из этого набора в порядке 
возрастания и без повторений.

1(Егор). Написать функцию mysort, принимающую в качестве параметра 
библиотечный вектор целых чисел и выводящую числа из этого вектора 
в порядке возрастания и без повторений.
 
1(Алексей). Написать функцию mysort, принимающую в качестве параметра 
строку целых чисел, в которую записаны целые числа через пробел. 
Функция должна выводить числа из этой строки в 
порядке возрастания и без повторений.


2. (Мария) Написать шаблонную функцию, принимающую два множества и
возвращающую их объединение.

2. (Егор) Написать шаблонную функцию, принимающую два множества и
возвращающую их разность.

2. (Алексей) Написать шаблонную функцию, принимающую два множества и
возвращающую их пересечение.


3. Написать функцию, вводящую с клавиатуры набор целых чисел
(в любом порядке, конец ввода -- Ctrl Z на пустой строчке), и выводящую числа
из этого набора, которые при вводе встречались не меньше двух раз.


4*. Написать функцию, вводящую с клавиатуры набор целых чисел
(в любом порядке, конец ввода -- Ctrl Z на пустой строчке), и выводящую числа из
этого набора, которые при вводе встречались ровно два раза.



Пейцзе

1. Написать шаблонную функцию, принимающую множество из элементов любого типа 
(из небольшого количества элементов) и возвращающую множество его подмножеств.


2. на выбор

10-2-43: Написать шаблонную функцию, принимающую отображение f: R -> R и возвращающую множество 2-цикловых точек отображения f, 
т. е. таких точек x, что существует y != x такой, что f(x)=y и f(y)=x (без вложенных циклов).

10-2-44: Написать шаблонную функцию, принимающую отображение f: R -> S и возвращающую число инверсий, 
т. е. таких пар (x,y), что x<y и f(x)>f(y).
 
10-2-47: Написать шаблонную функцию, проверяющую инъективность отображения (без вложенных циклов).


